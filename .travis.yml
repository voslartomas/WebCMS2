language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - hhvm

before_script:
  # Update Composer
  - composer self-update
  
  - mkdir tests/temp
  - mkdir tests/temp/log
  
  # Install
  - composer install
  
script:
  - phpunit --no-globals-backup --bootstrap tests/bootstrap.php tests
